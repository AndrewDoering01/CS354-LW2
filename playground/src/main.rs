fn main() {
    let mut sentence = String::from("This is a sentence");
    let first_word = find_first_word(&mut sentence);
}

fn findfirstWord(word: &String) -> String {
    let firstword = String::from(" ");
}
